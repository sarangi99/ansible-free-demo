---
- hosts: webapps
  become: yes
 
  tasks:
    - name: install apache2 on debian
      apt:
        name: apache2
        state: present 
     

    - name: start apache2 on debian
      service:
        name: apache2
        state: started
        enabled: yes
      